# Application name
spring.application.name=Dip-Computer

# server.port=8089

# === Local MySQL Configuration (Commented Out) ===
# spring.datasource.url=jdbc:mysql://localhost:3306/dip_computer?createDatabaseIfNotExist=true
# spring.datasource.username=root
# spring.datasource.password=root

# === Aiven MySQL Configurat# Use en## Use environment variables for database credentials. 
# In Render, you will set the following environment variables:
# DB_URL: jdbc:mysql://mysql-11b54541-dip-computer.j.aivencloud.com:23412/defaultdb?ssl-mode=REQUIRED
# DB_USERNAME: avnadmin
# DB_PASSWORD: <your_aiven_password>
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

# JPA & Hibernate Settings
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# JWT Secret for Authentication
# Use environment variable for JWT secret.
# In Render, you will set the following environment variable:
# JWT_SECRET: <your_jwt_secret>
# In Render, you will set the following environment variable:
# JWT_SECRET: <your_jwt_secret>
jwt.secret=${JWT_SECRET:defaultSecret}
# jwt.secret=sjdfisjdkhous98ey3ezjhiosuo8eqjjs[[epdj]]zxjhbckjhuegxcnjcjdjcbzbjjs

# Admin Credentials
# Use environment variables for admin credentials.
# In Render, you will set the following environment variables:
# ADMIN_USERNAME: <your_admin_username>
# # ADMIN_PASSWORD: <your_admin_password>
admin.username=${ADMIN_USERNAME:admin}
admin.password=${ADMIN_PASSWORD:password}
# admin.username=admin
# admin.password=admin

# SMTP Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=skillmate001@gmail.com
spring.mail.password=your_app_password
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
